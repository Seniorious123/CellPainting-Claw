# Release Directory

This directory holds release-facing helpers and serves as the anchor point for generated source bundles.

The actual packaged release output is written to `dist/` by:

- `scripts/run_release_smoke_test.sh`
- `scripts/build_release_bundle.sh`
