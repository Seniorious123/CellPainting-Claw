# Local Operator

This workspace is used to drive the validated Cell Painting pipeline library from natural-language requests.

Default expectations:

- run inside the local repository
- prefer the example config unless the user names a different config
- keep reports concrete and path-oriented
